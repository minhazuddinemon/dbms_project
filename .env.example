# Database Credentials
DB_USER=admission_user
DB_PASSWORD=secretpassword
DB_ROOT_PASSWORD=rootsecret
DB_NAME=admission_db

# For Docker workflow (docker compose up):
DB_DSN=admission_user:secretpassword@tcp(db:3306)/admission_db?parseTime=true&multiStatements=true

# For Strategy A workflow (go run on host):
# DB_DSN=admission_user:secretpassword@tcp(127.0.0.1:3307)/admission_db?parseTime=true&multiStatements=true
